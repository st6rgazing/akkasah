{
  "name": "akkasah-archive",
  "version": "1.0.0",
  "description": "Modern photography archive website inspired by Akkasah Center",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "test:security": "cd backend && python test_security.py",
    "test:security:ci": "cd backend && python test_security.py --url http://localhost:8000",
    "audit": "npm audit && cd frontend && npm audit",
    "audit:fix": "npm audit fix && cd frontend && npm audit fix",
    "security:check": "npm run audit && npm run test:security"
  },
  "keywords": ["photography", "archive", "react", "fastapi"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
